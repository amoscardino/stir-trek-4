import { TimeSlotFilterPipe } from './time-slot-filter.pipe';

describe('TimeSlotFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeSlotFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
